<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="noAnimTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <style name="ThemeOverlay.App.Dialog" parent="ThemeOverlay.MaterialComponents.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.5</item>
    </style>

    <style name="CheckboxRed" parent="Theme.ToDoListOnline">
        <item name="colorControlNormal">@color/myRed</item>
        <item name="colorControlActivated">@color/myRed</item>
    </style>
</resources>