<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="20dp" /> <!-- Радиус скругления углов (выберите нужный размер) -->
    <solid android:color="@android:color/white" /> <!-- Цвет фона (можно задать свой) -->
    <stroke
        android:width="1dp"/>
</shape>